{
  "name": "wordle-love",
  "version": "2.0.0",
  "private": true,
  "description": "Wordle game with Python backend and smart word tracking",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:backend:python": "cd backend_python && python server.py",
    "dev:frontend": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "start:python": "cd backend_python && python server.py"
  }
}
